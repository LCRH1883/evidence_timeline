<adonis:AdonisWindow x:Class="evidence_timeline.Views.StartDialog"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:adonis="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                     Title="Evidence Timeline"
                     SizeToContent="WidthAndHeight"
                     WindowStartupLocation="CenterOwner"
                     ResizeMode="NoResize"
                     MinWidth="320">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Welcome"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0 0 0 10" />
            <TextBlock Text="Create a new case or open an existing one."
                       FontSize="12"
                       Margin="0 0 0 18" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Create new case"
                        Width="150"
                        Padding="8 4"
                        Margin="0 0 10 0"
                        Click="OnCreateClicked" />
                <Button Content="Open existing case"
                        Width="150"
                        Padding="8 4"
                        Click="OnOpenClicked" />
            </StackPanel>
        </StackPanel>
    </Grid>
</adonis:AdonisWindow>
